<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - Real Estate Portal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-blue-100 to-gray-100 flex items-center justify-center min-h-screen p-4">

    <div class="bg-white shadow-2xl rounded-xl w-full max-w-4xl overflow-hidden">
        <!-- Two-column layout container -->
        <div class="flex flex-col md:flex-row">
            <!-- Left column - Image/Banner for desktop -->
            <div class="hidden md:block md:w-2/5 bg-cover bg-center" style="background-image: url('/api/placeholder/800/1200')">
                <div class="h-full w-full bg-blue-600 bg-opacity-70 p-8 flex flex-col justify-end">
                    <h3 class="text-white text-2xl font-bold">Reset Your Password</h3>
                    <p class="text-gray-100 mt-2">We'll help you get back into your account quickly and securely.</p>
                </div>
            </div>
            
            <!-- Right column - Form -->
            <div class="w-full md:w-3/5 p-6 sm:p-8">
                <!-- Header with logo placeholder -->
                <div class="flex items-center justify-between mb-6">
                    <div class="flex items-center">
                        <div class="w-10 h-10 bg-blue-600 rounded-full flex items-center justify-center">
                            <i class="fas fa-home text-white"></i>
                        </div>
                        <h2 class="text-2xl font-bold text-gray-800 ml-2">RealEstate<span class="text-blue-600">Portal</span></h2>
                    </div>
                    <a href="/login" class="text-sm text-blue-600 hover:underline">Back to login</a>
                </div>

                <h3 class="text-xl font-bold text-gray-800 mb-1">Forgot Your Password?</h3>
                <p class="text-gray-500 text-sm mb-6">Enter your email address below and we'll send you password reset instructions.</p>

                <!-- ✅ Success & Error Messages -->
                <div id="messageBox" class="hidden p-3 rounded-lg text-center font-semibold mb-4"></div>

                <form action="/reset-password" method="post" class="space-y-6">
                    <!-- Email -->
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none z-10">
                                {{!-- <i class="fas fa-envelope text-gray-400"></i> --}}
                            </div>
                            <input type="email" placeholder="your@email.com" name="email" class="w-full pl-10 p-3 bg-gray-50 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 focus:outline-none transition duration-200" required>
                        </div>
                    </div>

                    <!-- Reset Password Button -->
                    <div>
                        <button type="submit" class="w-full bg-blue-600 text-white font-medium py-3 px-4 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition duration-300 transform hover:-translate-y-1">
                            Send Reset Instructions
                        </button>
                    </div>

                    <!-- Divider -->
                    <div class="relative my-6">
                        <div class="absolute inset-0 flex items-center">
                            <div class="w-full border-t border-gray-200"></div>
                        </div>
                        <div class="relative flex justify-center text-sm">
                            <span class="px-2 bg-white text-gray-500">Or</span>
                        </div>
                    </div>

                    <!-- Contact Support -->
                    <div class="bg-gray-50 p-4 rounded-lg border border-gray-200">
                        <div class="flex items-start">
                            <div class="flex-shrink-0 mt-0.5">
                                <i class="fas fa-headset text-blue-600 text-xl"></i>
                            </div>
                            <div class="ml-3">
                                <h4 class="text-sm font-medium text-gray-800">Need help?</h4>
                                <p class="mt-1 text-sm text-gray-500">
                                    If you're having trouble accessing your account, our support team is here to help.
                                </p>
                                <a href="#" class="mt-2 inline-flex items-center text-sm font-medium text-blue-600 hover:text-blue-500">
                                    Contact Support
                                    <i class="fas fa-arrow-right ml-1 text-xs"></i>
                                </a>
                            </div>
                        </div>
                    </div>

                    <!-- Links -->
                    <div class="text-center space-y-2">
                        <p class="text-sm text-gray-500">
                            Remember your password? 
                            <a href="/login" class="text-blue-600 font-medium hover:underline">Log in</a>
                        </p>
                        <p class="text-sm text-gray-500">
                            Don't have an account? 
                            <a href="/register" class="text-blue-600 font-medium hover:underline">Sign up</a>
                        </p>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- ✅ JavaScript for Success/Error Message & Redirect -->
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // Get query parameters (to check for success/error messages)
            const urlParams = new URLSearchParams(window.location.search);
            const successMessage = urlParams.get("success");
            const errorMessage = urlParams.get("error");

            // Get message box
            const messageBox = document.getElementById("messageBox");

            if (successMessage) {
                messageBox.innerText = successMessage;
                messageBox.classList.add("bg-green-500", "text-white");
                messageBox.style.display = "block";

                // Redirect after 3 seconds
                setTimeout(() => window.location.href = "/login", 3000);
            }

            if (errorMessage) {
                messageBox.innerText = errorMessage;
                messageBox.classList.add("bg-red-500", "text-white");
                messageBox.style.display = "block";
            }
        });
    </script>

</body>
</html>